# ZAIos wpa_supplicant config — used by zaios-network service
ctrl_interface=/run/wpa_supplicant
ctrl_interface_group=netdev
update_config=1
ap_scan=1

# Networks are added dynamically by the shell UI via wpa_cli
